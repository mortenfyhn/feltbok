package io.github.mortenfyhn.feltbok

import androidx.compose.ui.graphics.Color

/** The flavor's brand colours (app chrome). Norway is moss green. */
object Brand {
    val primary = Color(0xFF5B7A2B)
    val dark = Color(0xFF41591D)
    val container = Color(0xFFDCE8C6)
}
