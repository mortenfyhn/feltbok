# Golden = a small REACTIVE regression list, not a catalog to fill in by hand.
#
# Most search behaviour is mechanical (every query is a prefix/suffix/typo/folded form of the bird's
# name) and is already measured, commonness-weighted, by the synthetic generator in SearchQueries.kt.
# So "type 'tårn' -> common tårn-bird" and "type 'meis' -> common -meis birds" need NO rows here -
# they're the general rule the synthetic set tests for all 600 species.
#
# Only add a row when you hit a real search result you're unhappy with and want locked so it can't
# regress - especially NON-mechanical cases the generator can't derive from the name (e.g. learned
# initialisms like rvt -> Rødvingetrost). The benchmark reports golden as its own pass-count, and
# lists the misses for the ship scorer, so each row stays visible.
#
# format: query,expectedNorsk   (# / blank lines ignored; query case & æ/ø/å don't matter; the
# expected name must exist exactly as in species.csv, or the row is silently skipped)
#
# (empty for now - add as you go)
sanger,Gransanger
a,Alke
konge,Fuglekonge
tårn,Tårnseiler
stor,Storspove
tybj,Tyvjo
