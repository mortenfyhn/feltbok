package io.github.mortenfyhn.feltbok

import androidx.compose.ui.graphics.Color

/** The flavor's brand colours (app chrome). Sweden is an Artportalen-style blue, to tell the
 *  Swedish app apart from the green Norwegian one at a glance. */
object Brand {
    val primary = Color(0xFF1E6FA8)
    val dark = Color(0xFF154E78)
    val container = Color(0xFFC8DEEE)
}
