# Build output
/app/build/
/.gradle/
/app/.cxx/
# Android SDK path — machine-specific
/local.properties

# LibreOffice/editor lock files
.~lock.*#

# Python
process/__pycache__/
.venv/

# Field data / generated sheets (keep templates, not outputs)
/recordings/
/observasjoner.xlsx
/observasjoner_review.xlsx
/eksempel*.xlsx
/probe_*.xlsx
# Locality lists (your spots + the harvested gazetteer; keep local)
/my_localities.csv
/localities_gazetteer.csv
/bird_species.csv
# Official locality table + its source archive (built by build_localities.py)
/localities.csv
/artsobs-dwca.zip
# Locality import-matching test sheet
/lokalitetstest.xlsx

# Local models (NB-Whisper CTranslate2 build)
/models/
